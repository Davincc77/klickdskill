# `.klickd v4.1 — Chimera` — candidate skill mapping (planning, NOT a catalog)

| | |
|---|---|
| **Status** | **Draft · NON-NORMATIVE · planning only** |
| **Track** | `.klickd v4.1` — Chimera (post-`v4.0.0` GA) |
| **Created** | 2026-05-27 |
| **Relates to** | [`docs/rfcs/RFC-009-chimera-v4.1.md`](../rfcs/RFC-009-chimera-v4.1.md) · [`docs/rfcs/chimera/README.md`](../rfcs/chimera/README.md) · [`docs/rfcs/chimera/packs/README.md`](../rfcs/chimera/packs/README.md) · [`docs/rfcs/chimera/frameworks/README.md`](../rfcs/chimera/frameworks/README.md) |
| **Companion** | [`README_V4_1.md`](./README_V4_1.md) (planning index) · [`V4_1_CANDIDATE_CHECKLIST.md`](./V4_1_CANDIDATE_CHECKLIST.md) (per-candidate review checklist) · [`V4_1_COMPETENCY_IDENTIFICATION_PROTOCOL.md`](./V4_1_COMPETENCY_IDENTIFICATION_PROTOCOL.md) (method for selecting and combining competencies per skill — merge gate) |

> **This document is non-normative.** It does **not** modify any current `SPEC.md` section, schema, SDK behaviour, vector, lock file, or RFC status. It introduces **no new normative field**. It does **not** publish anything (no tag, no `latest` on npm/PyPI, no DOI on Zenodo, no IANA action, no SDK bump, no `/klickdskill` catalog change).
>
> Every candidate below is **`candidate_mapped`** unless explicitly flagged otherwise. None of them are `ship_ready`. The mapping exists so reviewers can argue with **scope, naming, and framework anchors**.
>
> **Update 2026-05-27 — concrete artefacts now ship alongside this doc.** For every `candidate_mapped` row whose framework anchors are sufficient, a real `.klickd` file exists under [`examples/v4.1/x-klickd-skills/lite/`](../../examples/v4.1/x-klickd-skills/lite/) (Lot A) or [`examples/v4.1/x-klickd-skills/pro/`](../../examples/v4.1/x-klickd-skills/pro/) (Lot B, with `compact_index` loading strategy). The `needs_mapping` rows (A5, A6, A8, A14, A15) and the two `student`-sub-area rows (A2 `language`, A3 `exam`) have **no** artefact — they are documented here only. The validator at `scripts/validate_v4_1_candidate_mapping.py` enforces both directions.
>
> **Update 2026-05-27 (audit response) — rename pass applied.** Six artefact filenames were renamed and four canonical pack ids were tightened so that every filename stem matches its `pack` tail (audit W-1 / BLOCKER fix). `crypto-lite` was demoted to `needs_mapping` because no EU SKOS-published crypto-asset-literacy framework exists with DigComp / NICE / CIS-comparable maturity. Full mapping in §0.5 below.

### 0.5 Rename + defer table (audit response, 2026-05-27)

| # | Old filename | Old `pack` id | New filename | New `pack` id | Reason |
|---|---|---|---|---|---|
| 1 | `lite/work-lite.klickd` | `x.klickd/work` (Lot A sub-profile) | `lite/work-assistant.klickd` | `x.klickd/work_assistant` | Audit BLOCKER: `-lite` is a planning suffix, not a canonical name; filename tail and pack tail must match. ESCO S1.3/S1.4/S4 anchor a day-to-day work-assistant pack distinct from the full senior PM scope of `x.klickd/work` (P1). |
| 2 | `lite/media-lite.klickd` | `x.klickd/creator` (Lot A sub-profile) | `lite/media-planner.klickd` | `x.klickd/media_planner` | Audit BLOCKER. DigComp 3.1/3.2/3.3 + RFC-001 `media_profile` anchor a planning-focused media-literacy pack distinct from the full P1 `creator`. |
| 3 | `lite/streamer-lite.klickd` | `x.klickd/streamer` | `lite/streaming-creator.klickd` | `x.klickd/streaming_creator` | Audit BLOCKER. ESCO S1.5 + DigComp 4.3/4.2 + RFC-001 anchor the same competencies under a clearer creator-composition name. |
| 4 | `lite/crypto-lite.klickd` | `x.klickd/crypto_basics` | **DEFERRED — artefact removed** | `x.klickd/crypto_basics` (planning only) | Audit BLOCKER + user's strict rule "no invented names not grounded in Chimera mapping". No EU SKOS-published crypto-asset-literacy framework exists with the maturity required (NICE / CIS cover security hygiene generically but do not anchor "crypto" as a distinct competency class). `A8` reclassified `needs_mapping` until a framework anchor is agreed. |
| 5 | `pro/agent-security.klickd` | `x.klickd/agent_security` | `pro/llm-agent-security.klickd` | `x.klickd/llm_agent_security` | Audit BLOCKER. The scope is specifically LLM/agent-system security (prompt-injection literacy, tool-use sandboxing, eval discipline); naming makes that explicit. NICE Adversary Emulation / Secure Software Assessment + ENISA Penetration Tester + CIS controls remain the anchors. |
| 6 | `pro/ai-agent-builder.klickd` | `x.klickd/ai_agent_builder` | `pro/llm-agent-engineering.klickd` | `x.klickd/llm_agent_engineering` | Audit BLOCKER. ESCO S5.6.1 (AI / ML systems) + SFIA PROG/ARCH + WEF AI&big-data anchor engineering of LLM agents; "builder" is informal, "engineering" is the SFIA-aligned term. |
| 7 | `pro/iam-endpoint.klickd` | `x.klickd/iam_endpoint` | `pro/identity-access-management.klickd` | `x.klickd/identity_access_management` | Audit BLOCKER. Filename mashed two scopes (IAM and endpoint hardening) but the pack only carried IAM-aligned anchors (NICE Identity Management + CIS controls 5 / 6 / 9). Per user's strict rule "better fewer true skills than many dubious names", we keep one canonical IAM scope and drop CIS control 9 (Email and Web Browser Protections, an endpoint-hardening control). A future endpoint-hardening pack would need its own mapping + RFC step. |
| 8 | `lite/consumer-rights.klickd` | `x.klickd/consumer` | `lite/consumer-rights.klickd` | `x.klickd/consumer_rights` | Audit BLOCKER: filename tail / pack tail mismatch. `consumer_rights` is more descriptive and matches the filename. |
| 9 | `lite/social.klickd` | `x.klickd/social_literacy` | `lite/social-literacy.klickd` | `x.klickd/social_literacy` | Audit BLOCKER: filename tail / pack tail mismatch. Filename renamed to match the canonical `social_literacy` pack tail. |
| 10 | `pro/drone.klickd` | `x.klickd/drone_operator` | `pro/drone-operator.klickd` | `x.klickd/drone_operator` | Audit BLOCKER: filename tail / pack tail mismatch. Filename renamed to match the canonical `drone_operator` pack tail. |

The planning nicknames in the §1 / §2 tables below (`work-lite`, `media-lite`, etc.) are **kept** so the validator's required-nicknames roster still matches; the **canonical filename column** is the source of truth for what ships on disk. The same artefact file carries `_pack_metadata.renamed_from` so reviewers can trace history.

---

## 0. Reading guide

This document is a **mapping plan**, not a roster of finished work. It pairs a list of in-flight candidates (work-lite, language, exam, media-lite, agent-security, …) with the Chimera v4.1 invariants and proposes, for each candidate:

1. **Parent `carrier_pack`(s)** — which existing P0/P1 pack(s) from [RFC-009 §3 / §4](../rfcs/RFC-009-chimera-v4.1.md) this candidate sits under or composes with.
2. **Namespace + proposed canonical filename** — what the candidate would be called if (and only if) it ever passes the ten validation criteria of [RFC-009 §8](../rfcs/RFC-009-chimera-v4.1.md).
3. **Target carrier (user)** — who would actually carry this pack (a learner, a developer, a maintainer, a guardian, …). No PII (RFC-009 §8.4).
4. **Competencies to map** — which authoritative-framework anchors the candidate must resolve to (ESCO / WEF / O\*NET / DigComp / EQF / CEFR / NICE / ENISA / CIS / SFIA — see [`docs/rfcs/chimera/frameworks/README.md`](../rfcs/chimera/frameworks/README.md) §1).
5. **Gates + human veto** — RFC-002 defaults, `human_authority` posture (always `final_decision_owner ∈ {human_carrier, human_carrier_with_guardian}`).
6. **Source / evidence policy** — which RFC-002 §8b grounding shape and RFC-003 `verification_artifacts[]` reference the candidate would use.
7. **Size tier** — Lot A (lightweight, near current Chimera size; `tokens_estimate ≤ ~900`) or Lot B (advanced; +50% size allowance, `tokens_estimate ≤ ~1,350`, requires compact index / on-demand loading).
8. **Readiness status** — `candidate_mapped` (framework anchors identified, scaffold not yet authored), `needs_mapping` (framework anchors insufficient or contested — explicitly NOT `candidate_mapped`), or `ship_ready` (validation §8 fully satisfied — none of the candidates below is in this state).

The terms `carrier_pack`, `competency_pack`, `host_skill`, `base_transversal_core`, and `human_authority_layer` are used per the canonical Chimera vocabulary in [RFC-009 §0.1](../rfcs/RFC-009-chimera-v4.1.md). The forbidden phrasings of [RFC-009 §0.1.2](../rfcs/RFC-009-chimera-v4.1.md) apply throughout this document.

## 0.1 What this document does NOT do

- It does **not** create any `.klickd` pack file. All references to `x.klickd/<name>` below are **planning identifiers**, not artefacts on disk. No new file appears under `examples/v4/starter-skills/`, no new file appears under `docs/rfcs/chimera/packs/fixtures/`, no entry is added to `examples/v4/starter-skills/manifest.json`.
- It does **not** publish anything to npm / PyPI / Zenodo / IANA.
- It does **not** tag a release or update `CHANGELOG.md`.
- It does **not** introduce a `/klickdskill` listing, search index, API endpoint, or download surface for any of the candidates below. The no-catalog rule of [RFC-009 §7](../rfcs/RFC-009-chimera-v4.1.md) applies in full.
- It does **not** include any Klickd.app-specific carrier (`klickdapp.lu.klickd`, `klickdapp.fr.klickd`, `klickdapp.be.klickd`, `klickdapp.de.klickd`, "Kai Tutor", "Kai Mentor", any other `kai.*` host-side skill). Those are Klickd.app product artefacts that live under [`examples/v4/klickdapp-skills/`](../../examples/v4/klickdapp-skills/) and are explicitly out of scope here. The public `/klickdskill` catalog and any planning document under `docs/chimera/` MUST NOT reference them as candidate Chimera packs.
- It does **not** alter the v4.0 envelope, the existing canonical P0 starters (`user.klickd`, `student.klickd`, `research.klickd`, `coding.klickd` under [`examples/v4/starter-skills/`](../../examples/v4/starter-skills/)), or the P1 candidate identifiers fixed in [RFC-009 §4](../rfcs/RFC-009-chimera-v4.1.md) (`x.klickd/work`, `x.klickd/creator`, `x.klickd/gaming`, `x.klickd/bridge`, `x.klickd/mission`).

## 0.2 Sharp boundary: `candidate_mapped` vs `ship_ready`

| Status | Meaning | What is required | What is NOT yet required |
|---|---|---|---|
| `needs_mapping` | The candidate has not yet been anchored to a sufficient set of authoritative frameworks; reviewers cannot evaluate it against [RFC-009 §8.1](../rfcs/RFC-009-chimera-v4.1.md). | A `Notes / open questions` line in §1 / §2 explaining what is missing. | Anything else. The candidate stays parked until anchors land. |
| `candidate_mapped` | The candidate has an identified parent pack, a proposed canonical filename, a target carrier, a candidate set of framework anchors, a gate posture, and a size-tier slot. | All seven fields populated; reviewer can argue with scope, name, and anchors. | A scaffold under `docs/rfcs/chimera/packs/`. An offline SKOS/JSON-LD bundle. A strict JSON Schema. A round-trip vector. A `router_cost` row computed against real bytes. |
| `ship_ready` | The candidate has passed all **ten** validation criteria of [RFC-009 §8](../rfcs/RFC-009-chimera-v4.1.md). Eligible for a future promotion RFC + checklist gate (but **not** automatically eligible for catalog exposure — that is a separate decision per RFC-009 §7). | Everything in `candidate_mapped` **plus** scaffold, framework anchors fully resolved, offline bundle bytes, strict JSON Schema, round-trip vector, deterministic `router_cost`, static `forbidden_fields` check, pair-reviewer sign-off. | — |

**No candidate in this document is `ship_ready`.** A few — notably `personal-finance` / `budget` overlap, `family`, `wellbeing-lite` — are explicitly `needs_mapping`. The rest are `candidate_mapped`. Promotion past `candidate_mapped` is a separate per-pack RFC + scaffold under [`docs/rfcs/chimera/packs/`](../rfcs/chimera/packs/), gated by [`docs/rfcs/ACCEPTANCE_CHECKLIST_V4.md`](../rfcs/ACCEPTANCE_CHECKLIST_V4.md).

## 0.3 Size tiers (Lot A vs Lot B)

| Tier | Audience | `tokens_estimate` ceiling | Loading strategy | Examples (P0/P1, already mapped) |
|---|---|---|---|---|
| **Lot A — lightweight / user-lambda** | General public, learners, end-users. Loaded by default on top of `x.klickd/user`. | ≤ ~900 tokens (matches `x.klickd/student` heuristic of 850 — see [`docs/rfcs/chimera/packs/router_cost.md`](../rfcs/chimera/packs/router_cost.md) §2). | Full manifest in prompt; SKOS bundle resolved on demand. | `x.klickd/user` (600), `x.klickd/student` (850). |
| **Lot B — advanced / dev / pro** | Developers, researchers, compliance officers, professional users. Opted-in. | ≤ ~1,350 tokens (Lot A ceiling + 50%). Above this, the pack MUST split or move to a `temporary_overlay` model. | **Compact index in prompt**, full body lazy-loaded only when the `decision_router` activates the candidate. The compact index carries `pack`, `frameworks[]`, `competencies[]` IDs only (no full `prefLabel`), `gates`, `human_authority`, `router_cost`. Full bodies (mastery scales, evidence rules, framework subsets) load on demand. | Anticipated for `x.klickd/coding` (target ~850), `x.klickd/research` (target ~900); a stricter compact-index strategy applies for Lot B candidates below. |

The size tier is **descriptive**, not normative — it is a planning hint so reviewers can spot a candidate that would blow the seven-pack ceiling ([RFC-009 §5.3 / §5.4](../rfcs/RFC-009-chimera-v4.1.md)) if loaded alongside the always-on `x.klickd/user`.

**Byte-size budgets (2026-05-27 update — public Chimera v4.1 catalog only).** The on-disk file-size ceiling for artefacts under [`examples/v4.1/x-klickd-skills/{lite,pro}/`](../../examples/v4.1/x-klickd-skills/) is **Lite ≤ 12 KB** and **Pro ≤ 24 KB** (decimal KB by repo convention: 12 KB = 12 000 bytes; 24 KB = 24 000 bytes). The new ceilings are an **upper bound / capacity envelope**, NOT a target — artefacts stay as compact as their framework-anchored content allows. The `tokens_estimate` ceilings above (`≤ 900` Lite, `≤ 1 350` Pro) and the Pro compact_index discipline are preserved unchanged — the bigger byte budget gives Pro packs headroom for deeper carrier-state vocabulary in the **lazy-loaded body**, not in the prompt-loaded compact_index. These ceilings **do NOT apply** to Klickd.app student carriers under `examples/v4/klickdapp-skills/` or to any Kai host-side artefact — those live in different validation contracts. The validator (`scripts/validate_v4_1_candidate_mapping.py`, `TIER_BYTES_CEILING`) enforces the new budgets.

## 0.4 Naming convention used in this document

Per Vince's review, ambiguous names like `work-lite`, `student-lite`, `legal-lite` are **planning-only nicknames**; they MUST NOT appear as canonical `x.klickd/<name>` identifiers. Each candidate row below carries:

- a **working nickname** (the term the user requested in the planning conversation — `work-lite`, `language`, `exam`, …), and
- a **proposed canonical filename** under the strict `x.klickd/<name>` namespace, picked to:
  1. preserve already-mapped canonical files where they exist (`x.klickd/user`, `x.klickd/student`, `x.klickd/research`, `x.klickd/coding`, `x.klickd/security`, `x.klickd/legal`, the P1 set fixed in RFC-009 §4),
  2. avoid clashing with `core.<agent>.klickd` host-side cores from [RFC-006](../rfcs/RFC-006-agent-core.md) (any `core.*` name is host-side, never a carrier_pack),
  3. avoid clashing with Klickd.app product names (no `klickdapp.*`, no `kai.*`).

Where Vince's working name maps cleanly onto a parent pack, the canonical filename is the parent's identifier and the working name becomes a *sub-area* of that pack (e.g. `language` → `x.klickd/student` `language_proficiency` block; `exam` → `x.klickd/student` `exam_targets[]` block). Where Vince's working name names a new sibling pack, the canonical filename is a new `x.klickd/<name>` identifier and is flagged as a **proposed P1+ addition** that must clear an RFC step before authoring.

---

## 1. Lot A — public / user-lambda lightweight candidates

> **Scope:** small, near-Chimera-size packs that a general-public carrier could load on top of `x.klickd/user`. `tokens_estimate ≤ ~900`. Default loading strategy: full manifest in prompt.

| # | Working nickname | Proposed canonical filename | Parent pack(s) | Target carrier | Framework anchors (competencies to map) | Gates / human veto | Source / evidence policy | Size tier | Status |
|---|---|---|---|---|---|---|---|---|---|
| A1 | `work-lite` | `x.klickd/work_assistant` (filename `work-assistant.klickd`; composes with `x.klickd/work` (P1), NOT a redefinition) | `x.klickd/user` (always-on), `x.klickd/work` (P1) | Employed person / freelancer covering day-to-day deliverables, communication, planning — NOT senior PM (that stays in full `x.klickd/work`). | `esco` (management — entry-level subset: communication, planning, deliverable tracking), `lifecomp` (Social: collaboration; Personal: self-regulation), `wef` 2023 transversal ("active learning", "leadership and social influence" at entry tier). | RFC-002 defaults; `raise_only: true`. `human_authority.final_decision_owner = "human_carrier"`. Escalation to a senior reviewer for stakeholder-facing artefacts. | RFC-002 §8b grounding for any team-attributable claim; RFC-003 `verification_artifacts[]` shape (`pointer_only: true`) for deliverable references. | Lot A | `candidate_mapped` |
| A2 | `language` | (sub-area of `x.klickd/student` + `x.klickd/user`) — **NOT a new top-level pack** | `x.klickd/student`, `x.klickd/user` | Any carrier with `language_proficiency[]` (learner studying a second language; bilingual professional). | `cefr` 2020 Companion Volume (A1..C2 per language; can-do descriptors), `digcomp` 2.2 (1.1 / 1.2 information-literacy in the studied language). | RFC-002 defaults; consent required to surface CEFR level to a third party (host or evaluator). | Pointer-only evidence for self-assessed levels; framework-anchored evidence (school transcript, exam result reference) required for `level ≥ B2`. | Lot A | `candidate_mapped` |
| A3 | `exam` | (sub-area of `x.klickd/student.exam_targets[]`) — **NOT a new top-level pack** | `x.klickd/student` | Learner with one or more upcoming examinations (bac, Abitur, A-levels, IB, professional certification). | `eqf` (2017) for qualification level, `esco` (education — assessment subset), `digcomp` 2.2 (information literacy under exam constraints). Optional: jurisdiction-specific curriculum scheme (MENJE-LU, Éducation nationale FR, KMK DE, …) cited as `curriculum_refs[]`, NOT registered as a top-level `frameworks[]` scheme. | RFC-002 defaults; `gates.verification_gates_default.confirm` for exam-date / target-grade writes (carrier may pin); `human_authority` retains veto on tutor recommendations. | RFC-002 §8b grounding for grade claims; RFC-003 `verification_artifacts[]` (pointer-only) for past results. | Lot A | `candidate_mapped` |
| A4 | `media-lite` | `x.klickd/media_planner` (filename `media-planner.klickd`; composes with `x.klickd/creator` (P1), NOT a redefinition) | `x.klickd/user`, `x.klickd/creator` (P1) | End-user creator (hobbyist editor, occasional newsletter author, light social media poster) — NOT a journalist or full editorial role. | `esco` (creative occupations — entry-tier subset), RFC-001 `media_profile` references (citation-only, NOT a top-level `frameworks[]` scheme), `digcomp` 2.2 (3.1 developing digital content; 3.2 integrating and re-elaborating). | RFC-002 defaults; `gates.media_release` `confirm` for any third-party-identifiable content; `human_veto_policy` raised for minors-in-frame. | RFC-001 `media_profile` discipline; RFC-002 §8b grounding for attributions; RFC-003 `verification_artifacts[]` for source clips. | Lot A | `candidate_mapped` |
| A5 | `personal-finance` | `x.klickd/personal_finance` (**proposed new pack, requires P1+ RFC step**) | `x.klickd/user` | Adult carrier managing household finances, savings, basic budgeting, basic tax awareness — NOT investment advice, NOT regulated financial advice. | `esco` (administrative / financial-administration subset), `digcomp` 2.2 (4.3 protecting health & well-being for financial well-being framing). **Open: no European financial-literacy framework with the maturity of DigComp; OECD/INFE 2020 toolkit is an option but not a SKOS-published taxonomy.** | RFC-002 defaults; `confirm` gate on any external transfer or commitment write; mandatory escalation to a licensed advisor for anything resembling regulated advice; minor carriers default to `human_carrier_with_guardian`. | Pointer-only evidence; no inline transactional data; jurisdictional notice that this is **not** financial advice. | Lot A | `needs_mapping` — financial-literacy framework anchor unresolved. |
| A6 | `budget` | (sub-area of `x.klickd/personal_finance` once that pack clears mapping) — **NOT a separate pack** | `x.klickd/personal_finance` (when promoted) | Same carrier as A5, focused on month-to-month spend tracking. | Inherits from A5; adds `lifecomp` Personal (self-regulation) anchor. | Same as A5. | Same as A5. | Lot A | `needs_mapping` — depends on A5. |
| A7 | `consumer-rights` | `x.klickd/consumer_rights` (filename `consumer-rights.klickd`; **proposed new pack, requires P1+ RFC step**) | `x.klickd/user`, `x.klickd/legal` (P0) | Consumer carrier dealing with returns, warranty, distance-selling, GDPR data-subject rights — NOT litigation. | `esco` (legal — consumer-protection subset), citation references to EU Consumer Rights Directive 2011/83/EU, GDPR Art. 12–22 (citation-only, NOT a top-level `frameworks[]` scheme). | RFC-002 defaults; `human_authority.final_decision_owner = "human_carrier"`; mandatory escalation to a licensed lawyer when a dispute escalates past first written reply. | Pointer-only evidence; no inline contract text from third parties unless the carrier asserts rights to share. | Lot A | `candidate_mapped` |
| A8 | `crypto-lite` | `x.klickd/crypto_basics` (planning only — **artefact removed**; **needs_mapping**) | `x.klickd/user`, `x.klickd/security` (P0) | Adult carrier handling self-custody wallets, hardware-wallet hygiene, basic on-chain literacy — NOT trading strategy, NOT investment advice. | `nice` / `cis` cover security hygiene generically but do **not** anchor "crypto-asset literacy" as a distinct competency class. **No EU SKOS-published crypto-literacy framework exists with DigComp / NICE-comparable maturity.** | (irrelevant while deferred) | (irrelevant while deferred) | Lot A | `needs_mapping` — no crypto-literacy framework anchor. Artefact removed per audit BLOCKER + user's strict rule "no invented names not grounded in Chimera mapping". |
| A9 | `social` | `x.klickd/social_literacy` (filename `social-literacy.klickd`; **proposed new pack, requires P1+ RFC step**) | `x.klickd/user` | Carrier with active social-media use, interested in critical literacy, attention hygiene, disinformation resistance — NOT growth-hacking, NOT marketing. | `digcomp` 2.2 (1.2 evaluating data; 2.2 sharing through digital technologies; 2.4 collaborating; 4.3 protecting health & well-being), `lifecomp` (Social: communication, collaboration; Personal: self-regulation), `wef` 2023 ("active learning", "resilience, stress tolerance and flexibility"). | RFC-002 defaults; `confirm` gate on any post that names a third party or minor; minor carriers default to `human_carrier_with_guardian`. | RFC-002 §8b grounding for any factual claim a draft post relies on. | Lot A | `candidate_mapped` |
| A10 | `artist` | `x.klickd/artist` (**proposed new pack, requires P1+ RFC step; distinct from `creator`**) | `x.klickd/user`, `x.klickd/creator` (P1, compose, do NOT fold into) | Independent visual / musical / performing artist — practice rather than production-line creator. | `esco` (creative occupations — practice-focused subset, distinct from media-production subset of `creator`), `lifecomp` (Personal: self-regulation, well-being), `digcomp` 2.2 (3.1 developing digital content). | RFC-002 defaults; `human_authority.final_decision_owner = "human_carrier"`; explicit consent for use of carrier's work as model training data is REQUIRED to be in `evidence_policy.attestation_shape_ref`. | Pointer-only evidence for works; explicit attribution shape; AI-training-consent posture declared (default: **opt-out**). | Lot A | `candidate_mapped` |
| A11 | `streamer-lite` | `x.klickd/streaming_creator` (filename `streaming-creator.klickd`; **proposed new pack, requires P1+ RFC step**) | `x.klickd/user`, `x.klickd/creator` (P1) | Live-streaming carrier (gaming, IRL, study-with-me) — entry tier, not a full broadcasting professional. | `esco` (creative — live performance subset), `digcomp` 2.2 (4.3 protecting health & well-being; 2.2 sharing), RFC-001 `media_profile` references (citation-only). | RFC-002 defaults; `confirm` gate for revealing real-world location or minors-on-stream; `human_veto_policy` strict on chat-driven dares; minor carriers default to `human_carrier_with_guardian`. | RFC-001 `media_profile` discipline; pointer-only evidence for clips; explicit consent for guests on-stream. | Lot A | `candidate_mapped` |
| A12 | `game-literacy` | `x.klickd/game_literacy` (**proposed new pack, requires P1+ RFC step; distinct from `x.klickd/gaming` (P1) and `x.klickd/game_design` in Lot B**) | `x.klickd/user`, `x.klickd/gaming` (P1) | General-public carrier learning to read games as media — genres, rules, mechanics, time-budget hygiene — NOT competitive play, NOT game development. | `esco` (leisure / sport — game-play subset), `wef` 2023 ("complex problem solving"), `digcomp` 2.2 (4.3 protecting health & well-being for screen-time). | RFC-002 defaults; `confirm` gate on any in-game purchase write; minor carriers default to `human_carrier_with_guardian`; `gates.time_budget` declared. | Pointer-only evidence; no inline screenshots unless declared by carrier. | Lot A | `candidate_mapped` |
| A13 | `parent-gaming` | `x.klickd/parent_gaming` (**proposed new pack, requires P1+ RFC step**) | `x.klickd/user`, `x.klickd/gaming` (P1), `x.klickd/security` (P0, compose for safety) | Parent / guardian carrier supporting a minor's gaming — rating literacy (PEGI / ESRB), screen-time discipline, online-safety hygiene. NOT a control panel; the pack carries the **parent carrier's** state, not the child's. | `esco` (educational support occupations), `digcomp` 2.2 (4.1 protecting devices; 4.2 protecting personal data and privacy; 4.3 well-being), `lifecomp` (Social: empathy). | RFC-002 defaults; `human_authority.final_decision_owner = "human_carrier"` (the parent); for advice that would affect the minor's own carrier file, `human_carrier_with_guardian` posture is REQUIRED on the minor's pack — this candidate cannot override that. | Pointer-only evidence; no inline child PII; jurisdictional notice (PEGI EU vs ESRB US vs USK DE). | Lot A | `candidate_mapped` |
| A14 | `wellbeing-lite` | `x.klickd/wellbeing` (**proposed new pack, requires P1+ RFC step**) | `x.klickd/user` | Adult carrier interested in self-regulation, sleep hygiene, stress literacy — explicitly NOT clinical mental-health support, NOT diagnosis, NOT therapy. | `lifecomp` (Personal: self-regulation, well-being), `digcomp` 2.2 (4.3 protecting health and well-being), `wef` 2023 ("resilience, stress tolerance and flexibility"). **Open: no consensus SKOS-published health-literacy framework with EU-level adoption; WHO health-literacy framework is referenced but not SKOS-published.** | RFC-002 defaults; **mandatory `block` on any wording resembling clinical diagnosis, treatment recommendation, or medication advice**; mandatory escalation to a licensed clinician when carrier signals distress; minor carriers default to `human_carrier_with_guardian`. | Pointer-only evidence; no inline health PII; explicit "not a medical device" notice. | Lot A | `needs_mapping` — health-literacy framework anchor unresolved. |
| A15 | `family` | `x.klickd/family` (**proposed new pack, requires P1+ RFC step**) | `x.klickd/user` | Adult carrier coordinating household / multi-generation logistics — NOT a multi-user shared file; the pack carries the **carrier's** family-coordinator state. | `esco` (transversal — planning, communication), `lifecomp` (Social: empathy, collaboration). **Open: no European "family literacy" framework with EU-level adoption.** | RFC-002 defaults; `confirm` gate on any write that names another household member; consent posture required for naming minors; the pack MUST NOT carry data about other people without their consent (RFC-009 §8.4 PII rule applies). | Pointer-only evidence; explicit per-person consent shape required before naming family members. | Lot A | `needs_mapping` — taxonomy of "family literacy" unresolved; PII-of-others risk explicitly flagged. |

### 1.1 Lot A notes & open questions

- **Sub-profiles vs new packs.** `work-lite` (A1) and `media-lite` (A4) are explicitly **sub-profiles of the existing P1 packs** `x.klickd/work` and `x.klickd/creator`. They are NOT new sibling packs. Future authoring lands inside the parent pack's scaffold under `docs/rfcs/chimera/packs/work.md` / `creator.md` with a `profile: "lite"` selector (illustrative; no schema yet).
- **`language` (A2) and `exam` (A3) are NOT separate packs.** They are sub-areas of `x.klickd/student` (already pinned in [`docs/rfcs/chimera/packs/student.md`](../rfcs/chimera/packs/student.md) §2). The mapping here is so reviewers do not propose them as siblings later.
- **`personal-finance` / `budget` (A5/A6) blocked on framework anchor.** No EU SKOS-published financial-literacy framework exists with DigComp maturity. The current draft proposes citing OECD/INFE 2020 toolkit and EBA financial-literacy joint communications as **citations**, not as `frameworks[]` schemes. Until reviewers agree a path, both stay `needs_mapping`.
- **`wellbeing-lite` (A14) blocked on framework anchor.** Same shape of problem as A5 for the health-literacy side. The candidate cannot ship before the clinical-vs-non-clinical line is anchored (the `block` gate above is the minimum; the framework anchor is the open question).
- **`family` (A15) blocked on framework + PII-of-others.** A `family` pack that names other people instantly leaks PII under RFC-009 §8.4 unless an explicit per-person consent shape is part of the pack. The current draft requires this shape; until one is reviewed, the candidate stays `needs_mapping`.
- **`crypto-lite` (A8) keeps strict gates from `x.klickd/security`.** The pack inherits `security`'s blast-radius reasoning; reviewers MUST NOT relax the `block` gate on seed-phrase / private-key surfaces.

---

## 2. Lot B — advanced / dev / pro candidates

> **Scope:** advanced packs for developers, researchers, compliance officers, and professional users. `tokens_estimate ≤ ~1,350` (+50% allowance vs Lot A). Loading strategy: **compact index in prompt + on-demand body**. The compact index carries `pack`, `frameworks[]` IDs, `competencies[]` IDs (no full `prefLabel`), `gates`, `human_authority`, `router_cost`. Full bodies (mastery scales, evidence rules, framework subsets) load only when the `decision_router` activates the pack.

| # | Working nickname | Proposed canonical filename | Parent pack(s) | Target carrier | Framework anchors (competencies to map) | Gates / human veto | Source / evidence policy | Size tier | Status |
|---|---|---|---|---|---|---|---|---|---|
| B1 | `agent-security` | `x.klickd/llm_agent_security` (filename `llm-agent-security.klickd`; **proposed new pack, requires P1+ RFC step; composes with `x.klickd/security` (P0)**) | `x.klickd/user`, `x.klickd/security` (P0), `x.klickd/coding` (P0) | Engineer / SRE / red-teamer building or operating LLM agents and MCP / tool-using systems — prompt-injection literacy, tool-use sandboxing, eval discipline. | `nice` (Adversary Emulation, Secure Software Assessment, Vulnerability Assessment), `enisa` ECSF 1.0 (Cybersecurity Risk Manager, Penetration Tester), `cis` v8 (Application Software Security, Penetration Testing), `esco` (ICT security — software-engineering subset). | RFC-002 defaults; `confirm` gate on any write that would deploy agent capability change; mandatory `block` on any default that disables prompt-injection screening or memory-write guards. | RFC-002 §8b grounding for vuln claims; RFC-003 `verification_artifacts[]` shape for repros (pointer-only); explicit responsible-disclosure posture. | Lot B (compact index) | `candidate_mapped` |
| B2 | `ai-agent-builder` | `x.klickd/llm_agent_engineering` (filename `llm-agent-engineering.klickd`; **proposed new pack, requires P1+ RFC step; composes with `x.klickd/coding` (P0) and `x.klickd/llm_agent_security` (B1)**) | `x.klickd/user`, `x.klickd/coding` (P0), `x.klickd/llm_agent_security` (B1) | Developer building production LLM agents — tool design, eval harness, observability, cost discipline. | `esco` (ICT — software development + AI subset), `sfia` 8 (PROG, ARCH, METL — by id-link only unless licence held), `onet` 28.0 (15-1252.00 Software Developers cross-walk), `wef` 2023 ("AI and big data", "programming"). | RFC-002 defaults; `confirm` gate on any agent-deployment write; **mandatory `block` on any default that disables eval gating before a model-version bump**. | RFC-002 §8b grounding for capability claims; RFC-003 `verification_artifacts[]` for eval results; explicit cost / token-budget attestation shape. | Lot B (compact index) | `candidate_mapped` |
| B3 | `iam-endpoint` | `x.klickd/identity_access_management` (filename `identity-access-management.klickd`; **proposed new pack, requires P1+ RFC step; composes with `x.klickd/security` (P0)**) | `x.klickd/user`, `x.klickd/security` (P0) | Identity & access engineer covering account / access management, MFA, hardware-token discipline, session hygiene. Endpoint-hardening (EDR/MDM/email-and-web protections) is **out of scope**; a separate pack would need its own mapping + RFC step. | `nice` (Identity Management, Defensive Cybersecurity), `enisa` ECSF 1.0 (Cyber Incident Responder, Cybersecurity Implementer), `cis` v8 (Account Management, Access Control Management, Email and Web Browser Protections). | RFC-002 defaults; `confirm` gate on any IAM-write surface; **mandatory `block` on any default that lowers MFA or hardware-token requirements**. | RFC-002 §8b grounding for incident claims; RFC-003 `verification_artifacts[]` for log refs (pointer-only); never inline session tokens. | Lot B (compact index) | `candidate_mapped` |
| B4 | `release-engineer` | `x.klickd/release_engineer` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/coding` (P0)**) | `x.klickd/user`, `x.klickd/coding` (P0), `x.klickd/security` (P0, compose for supply-chain) | Release engineer / build owner — supply-chain hygiene, SBOM, reproducible builds, signing, rollback discipline. | `esco` (ICT — software-deployment subset), `sfia` 8 (RELM, CFMG, PORT — by id-link), `nice` (Software Development, Supply Chain Risk Management), `cis` v8 (Inventory and Control of Software Assets, Data Recovery). | RFC-002 defaults; `confirm` gate on any production-release write; **mandatory `block` on any default that disables artefact signing or SBOM emission**; rollback path declared. | RFC-002 §8b grounding for release claims; RFC-003 `verification_artifacts[]` for signed artefacts; SLSA-level posture declared. | Lot B (compact index) | `candidate_mapped` |
| B5 | `trust-evidence` | `x.klickd/trust_evidence` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0) | Trust & safety analyst / evidence-desk operator — claim grounding, chain-of-custody, escalation discipline. | `esco` (research — investigation subset; legal — evidence-handling subset), `digcomp` 2.2 (1.2 evaluating data; 1.3 managing data), RFC-002 §8b grounding rules (pack-internal, not a `frameworks[]` scheme). | RFC-002 defaults; `confirm` gate on any third-party-attributable claim; mandatory escalation to legal counsel when escalation touches a data-subject right. | RFC-002 §8b grounding; RFC-003 `verification_artifacts[]` (pointer-only, with chain-of-custody attestation shape). | Lot B (compact index) | `candidate_mapped` |
| B6 | `eu-ai-act` | `x.klickd/eu_ai_act` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/legal` (P0) | Compliance officer / DPO / engineer mapping a system to the EU AI Act — risk class, obligations, transparency, post-market monitoring. | `esco` (legal — regulatory-compliance subset), citation references to EU AI Act (Regulation 2024/1689), Annexes I–IV (citation-only, NOT a top-level `frameworks[]` scheme), ENISA AI threat landscape (citation-only). | RFC-002 defaults; `confirm` gate on any compliance-attestation write; **mandatory `block` on any default that classifies a system below its evidenced risk class**. | RFC-002 §8b grounding for risk-class claims; RFC-003 `verification_artifacts[]` for technical documentation refs; mandatory escalation to qualified legal counsel for definitive risk-class decisions. | Lot B (compact index) | `candidate_mapped` |
| B7 | `gdpr-readiness` | `x.klickd/gdpr_readiness` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/legal` (P0) | DPO / privacy engineer running a GDPR-readiness assessment — Art. 30 records, DPIA, data-subject rights, transfer mechanisms. | `esco` (legal — data-protection subset), citation references to GDPR (Regulation 2016/679), EDPB guidelines (citation-only), ENISA "Handbook on Security of Personal Data Processing" (citation-only). | RFC-002 defaults; `confirm` gate on any DPIA / Art. 30 write; **mandatory `block` on any default that omits a transfer-mechanism check for non-EU processors**. | RFC-002 §8b grounding for compliance claims; RFC-003 `verification_artifacts[]` for DPIA refs; mandatory escalation to DPO / counsel. | Lot B (compact index) | `candidate_mapped` |
| B8 | `contract-review` | `x.klickd/contract_review` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/legal` (P0) | Legal-adjacent reviewer (in-house counsel, contract manager, founder) reading commercial contracts — clause spotting, risk flagging, escalation. NOT legal advice. | `esco` (legal — contract-law / commercial-law subset), `onet` 28.0 (23-1011.00 Lawyers cross-walk for ladder of competence). | RFC-002 defaults; `confirm` gate on any clause-flag write that the carrier intends to send to the counterparty; **mandatory escalation to a licensed lawyer** for redlines that change liability, indemnity, or termination posture. | RFC-002 §8b grounding for clause claims; RFC-003 `verification_artifacts[]` for clause-citation refs (pointer-only, no inline confidential contract text without carrier attestation of rights). | Lot B (compact index) | `candidate_mapped` |
| B9 | `privacy-product` | `x.klickd/privacy_product` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/legal` (P0) and `x.klickd/coding` (P0)**) | `x.klickd/user`, `x.klickd/legal` (P0), `x.klickd/coding` (P0) | Product manager / engineer applying privacy-by-design to a product — data flows, retention, lawful basis, consent UX, data-subject-rights surfaces. | `esco` (ICT — privacy-engineering subset; legal — data-protection subset), `digcomp` 2.2 (4.2 protecting personal data and privacy), citation references to GDPR Art. 25 (citation-only), ENISA "Privacy by Design" (citation-only). | RFC-002 defaults; `confirm` gate on any data-flow write that crosses a processor boundary; mandatory escalation to DPO when retention or lawful basis is ambiguous. | RFC-002 §8b grounding for data-flow claims; RFC-003 `verification_artifacts[]` for ROPA / DPIA refs. | Lot B (compact index) | `candidate_mapped` |
| B10 | `evidence-desk` | `x.klickd/evidence_desk` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0) and `x.klickd/trust_evidence` (B5)**) | `x.klickd/user`, `x.klickd/research` (P0), `x.klickd/trust_evidence` (B5) | Operator running an evidence-intake / triage desk (journalism investigations, OSINT, civic accountability) — submission triage, source protection, claim grounding, chain-of-custody. | `esco` (research — investigation subset; creative — investigative-journalism subset), `digcomp` 2.2 (1.2 evaluating data; 1.3 managing data; 4.2 protecting personal data and privacy). | RFC-002 defaults; **mandatory `block` on any default that would reveal source identity**; `confirm` gate on any public-attribution write; mandatory escalation to editor / counsel before public release. | RFC-002 §8b grounding (strict); RFC-003 `verification_artifacts[]` with source-protection attestation shape; pointer-only; offline-friendly. | Lot B (compact index) | `candidate_mapped` |
| B11 | `policy-analyst` | `x.klickd/policy_analyst` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0) and `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0), `x.klickd/legal` (P0) | Public-policy analyst — legislative tracking, stakeholder mapping, comparative-jurisdiction analysis, briefing-note discipline. | `esco` (legal — public-administration subset; research — policy-analysis subset), `wef` 2023 ("analytical thinking", "systems thinking"), `onet` 28.0 (19-3094.00 Political Scientists cross-walk). | RFC-002 defaults; `confirm` gate on any attribution to a named legislator or institution; mandatory escalation to counsel when the analysis informs a regulated submission. | RFC-002 §8b grounding for legislative claims; RFC-003 `verification_artifacts[]` for legislative refs (pointer-only, with stable URLs). | Lot B (compact index) | `candidate_mapped` |
| B12 | `second-brain` | `x.klickd/second_brain` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0) | Knowledge-worker carrier running a long-running notes / PKM practice — capture discipline, citation hygiene, atomic notes, link integrity. NOT an external store; the pack carries the carrier's PKM **state**, not their notes. | `esco` (research — knowledge-management subset), `digcomp` 2.2 (1.3 managing data; 3.3 copyright and licences), `lifecomp` (Learning to Learn: managing learning). | RFC-002 defaults; `confirm` gate on any write that exports notes to a third party; pack MUST NOT contain inline note content (`pointer_only: true`). | Pointer-only evidence; explicit note-pointer shape (not the note bodies themselves); citation hygiene attestation. | Lot B (compact index) | `candidate_mapped` |
| B13 | `literature-review` | `x.klickd/literature_review` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0) | Researcher / postgraduate carrier running a systematic-or-narrative literature review — search strategy, inclusion criteria, risk-of-bias assessment, citation hygiene. | `esco` (research — literature-review subset), `digcomp` 2.2 (1.2 evaluating data; 1.3 managing data), citation references to PRISMA 2020 (citation-only). | RFC-002 defaults; `confirm` gate on any inclusion / exclusion write; mandatory escalation to supervisor / second reviewer for risk-of-bias assessment. | RFC-002 §8b grounding for inclusion claims; RFC-003 `verification_artifacts[]` for citation refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B14 | `project-operator` | `x.klickd/project_operator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/work` (P1) and `x.klickd/mission` (P1)**) | `x.klickd/user`, `x.klickd/work` (P1), `x.klickd/mission` (P1) | Senior operator running multi-stakeholder projects — risk register, dependency tracking, escalation discipline, exit-condition declaration. Distinct from `x.klickd/work` (general PM) by mission-scoped boundary discipline. | `esco` (management — project-management subset), `wef` 2023 ("resilience, stress tolerance and flexibility", "leadership and social influence"), `onet` 28.0 (11-3071.04 Supply Chain Managers cross-walk for ladder of competence). | RFC-002 defaults; `confirm` gate on any stakeholder-write that crosses an org boundary; mandatory escalation for any timeline change that violates an exit condition. | RFC-002 §8b grounding for status claims; RFC-003 `verification_artifacts[]` for deliverable refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B15 | `drone` | `x.klickd/drone_operator` (filename `drone-operator.klickd`; **proposed new pack, requires P1+ RFC step; composes with `x.klickd/security` (P0) and `x.klickd/legal` (P0) for airspace law**) | `x.klickd/user`, `x.klickd/security` (P0), `x.klickd/legal` (P0) | Civil drone (UAV) operator — pre-flight discipline, airspace literacy, no-fly zones, incident reporting. NOT military, NOT commercial heavy-lift. | `esco` (transport — aviation / UAV subset), citation references to EU Regulation 2019/947 (citation-only, NOT a top-level `frameworks[]` scheme; jurisdictional regulators for non-EU). | RFC-002 defaults; **mandatory `block` on any default that overrides a no-fly-zone declaration**; `confirm` gate on any first-flight-in-area write; minor carriers default to `human_carrier_with_guardian` for any flight write. | RFC-002 §8b grounding for airspace claims; RFC-003 `verification_artifacts[]` for incident refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B16 | `mission-control` | `x.klickd/mission_control` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/mission` (P1)**) | `x.klickd/user`, `x.klickd/mission` (P1), `x.klickd/security` (P0) | Operator running a time-critical multi-system mission — incident command, status declaration, comms discipline, escalation ladder. Distinct from `x.klickd/mission` (mission carrier-state) by adding the **operator-side** discipline of running the mission. | `esco` (management — operations subset; security — incident-management subset), `nice` (Incident Response), `enisa` ECSF 1.0 (Cyber Incident Responder), `wef` 2023 ("complex problem solving"). | RFC-002 defaults; `confirm` gate on any status-up / status-down declaration; **mandatory `block` on any default that bypasses an escalation step**; explicit comms discipline (no inline confidential channel content). | RFC-002 §8b grounding for status claims; RFC-003 `verification_artifacts[]` for incident-log refs (pointer-only). | Lot B (compact index) | `candidate_mapped` |
| B17 | `game-design` | `x.klickd/game_design` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/creator` (P1) and `x.klickd/coding` (P0)**) | `x.klickd/user`, `x.klickd/creator` (P1), `x.klickd/coding` (P0) | Game designer / systems designer — mechanic / dynamic / aesthetic literacy, playtesting discipline, accessibility, ESRB / PEGI rating literacy. Distinct from `x.klickd/game_literacy` (A12) by author-vs-reader axis. | `esco` (creative — game-design subset; ICT — game-development subset), `sfia` 8 (DESN, RFEN — by id-link), `onet` 28.0 (15-1255.00 Web and Digital Interface Designers cross-walk), `wef` 2023 ("creativity, originality and initiative"). | RFC-002 defaults; `confirm` gate on any monetisation-design write affecting minors; **mandatory `block` on any default that disables accessibility-review or rating-self-classification**. | RFC-002 §8b grounding for design claims; RFC-003 `verification_artifacts[]` for playtest refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B18 | `rights-guard` | `x.klickd/rights_guard` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/legal` (P0) and `x.klickd/creator` (P1)**) | `x.klickd/user`, `x.klickd/legal` (P0), `x.klickd/creator` (P1) | Rights / licensing manager — IP / copyright literacy, licence-compatibility analysis, attribution discipline, DMCA / EU Copyright Directive Art. 17 awareness. | `esco` (legal — intellectual-property subset; creative — rights-management subset), citation references to EU Copyright Directive 2019/790 (citation-only), Berne Convention (citation-only), Creative Commons 4.0 (citation-only). | RFC-002 defaults; `confirm` gate on any licence-classification write; **mandatory escalation to qualified IP counsel** for any redistribution decision that touches commercial use. | RFC-002 §8b grounding for licence claims; RFC-003 `verification_artifacts[]` for licence refs (pointer-only, with stable URLs to upstream licence texts). | Lot B (compact index) | `candidate_mapped` |
| B19 | `video-production-pipeline` | `x.klickd/video_production_pipeline` (filename `video-production-pipeline.klickd`; **proposed new pack, requires P1+ RFC step; composes with `x.klickd/media_planner` (A4), `x.klickd/creator` (P1), `x.klickd/research` (P0), `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/media_planner` (A4 candidate), `x.klickd/creator` (P1), `x.klickd/research` (P0), `x.klickd/legal` (P0) | Advanced creator / marketing engineer producing demo and marketing videos for a software product (e.g. a `.klickd` skill) — script, storyboard, shot list, prompt list, voice-over, subtitles, timing, render plan, provenance, rights, QA, benchmark reproducibility. NOT auto-publish-to-social; NOT a broadcast editor. | `esco` (creative — media-production subset; ICT — software-development subset), `sfia` 8 (DESN, PROG — by id-link), `digcomp` 2.2 (3.1 developing digital content; 3.3 copyright and licences), `onet` 28.0 (27-3041.00 Editors cross-walk). Citation-only: FFmpeg, SRT, MoneyPrinterTurbo audit reference (NOT registered as `frameworks[]`). | RFC-002 defaults; **mandatory `block`** on auto-publish-to-social, real-person likeness/voice, copyrighted-asset use, product-performance claims without evidence, and risky-provider use (e.g. g4f / pollinations); `confirm` gate on paid-API spend over carrier threshold and on stock-media licence writes. | RFC-002 §8b grounding for product-performance and rights claims (every benchmark / outcome claim shown or spoken in the produced video MUST cite a reproducible benchmark artefact pointer); RFC-003 `verification_artifacts[]` for asset provenance (per-asset license, source URL, retrieval timestamp, rights model); pointer-only; no undocumented bundled assets. Stock-media visual layer is OPTIONAL — primary visual layer for product demos is captured product footage. | Lot B (compact index) | `candidate_mapped` |
| B20 | `product-manager` | `x.klickd/product_manager` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/work` (P1) and `x.klickd/coding` (P0)**) | `x.klickd/user`, `x.klickd/work` (P1), `x.klickd/coding` (P0) | Product manager driving roadmap, discovery, and delivery for a software product — stakeholder alignment, prioritisation, evidence-based decisions. NOT a senior people-manager role. | `esco` (management — S4 product/portfolio subset), `sfia` 8 (PROD, RLMT — by id-link), `onet` 28.0 (11-2021.00 Marketing Managers cross-walk for ladder of competence), `wef` 2023 ("leadership and social influence", "analytical thinking"). | RFC-002 defaults; `confirm` gate on external roadmap commitments and on pricing-change writes; **mandatory `block` on any default that lets a feature decision skip its evidence artefact**. | RFC-002 §8b grounding for product claims; RFC-003 `verification_artifacts[]` for research / metric pointers; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B21 | `ux-researcher` | `x.klickd/ux_researcher` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0) | UX researcher running mixed-method discovery (interviews, usability testing, surveys, diary studies) — participant consent, bias mitigation, evidence-grounded reporting. | `esco` (research — user-research subset), `sfia` 8 (URCH, HCEV — by id-link), `onet` 28.0 (15-1255.00 Web and Digital Interface Designers cross-walk), `digcomp` 2.2 (1.2 evaluating data, information and digital content). | RFC-002 defaults; **mandatory `block`** on research without recorded participant consent and on raw-PII export outside the research perimeter; `confirm` gate on participant re-contact writes. | RFC-002 §8b grounding for finding claims; RFC-003 `verification_artifacts[]` for study refs (pointer-only, consent posture declared). | Lot B (compact index) | `candidate_mapped` |
| B22 | `data-analyst` | `x.klickd/data_analyst` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0) | Data analyst building dashboards, reports, and ad-hoc analyses — statistical literacy, query discipline, reproducibility. NOT a data scientist building ML models. | `esco` (ICT — data-analysis subset, S5.5), `sfia` 8 (DTAN, INAN — by id-link), `onet` 28.0 (15-2051.00 Data Scientists cross-walk), `wef` 2023 ("analytical thinking", "AI and big data"). | RFC-002 defaults; `confirm` gate on production-dataset writes; **mandatory `block`** on de-anonymisation-guard skip and on silent metric-definition changes. | RFC-002 §8b grounding for analytical claims; RFC-003 `verification_artifacts[]` for query / dataset version refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B23 | `api-integrator` | `x.klickd/api_integrator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/coding` (P0)**) | `x.klickd/user`, `x.klickd/coding` (P0) | Engineer integrating third-party APIs and building webhook / event consumers — idempotency, retry budgets, secret hygiene, vendor change discipline. | `esco` (ICT — software-development subset), `sfia` 8 (PROG, HSIN, METL — by id-link), `onet` 28.0 (15-1254.00 Web Developers cross-walk), `digcomp` 2.2 (3.4 programming). | RFC-002 defaults; `confirm` gate on retry-budget overrides and on major-version vendor SDK bumps; **mandatory `block` on any vendor secret reaching source or log surface**. | RFC-002 §8b grounding for capability claims; RFC-003 `verification_artifacts[]` for contract-test refs; pointer-only; never inline secrets. | Lot B (compact index) | `candidate_mapped` |
| B24 | `devops-operator` | `x.klickd/devops_operator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/coding` (P0) and `x.klickd/security` (P0)**) | `x.klickd/user`, `x.klickd/coding` (P0), `x.klickd/security` (P0) | DevOps / SRE operator running CI/CD, observability, incident-readiness. Distinct from `release_engineer` (B4) by operating-day-to-day scope vs supply-chain-artefact scope. | `esco` (ICT — software-deployment and infrastructure subsets), `sfia` 8 (ITOP, SCAD, AVMT — by id-link), `onet` 28.0 (15-1244.00 Network and Computer Systems Administrators cross-walk), `nice` (Operate and Maintain — Network Operations). | RFC-002 defaults; `confirm` gate on production-deploy writes; **mandatory `block`** on prolonged alert mutes outside declared maintenance and on silent IaC drift apply. | RFC-002 §8b grounding for status claims; RFC-003 `verification_artifacts[]` for run / log refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B25 | `security-incident-response` | `x.klickd/security_incident_response` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/security` (P0)**) | `x.klickd/user`, `x.klickd/security` (P0) | Security incident responder running detection, triage, containment, eradication, recovery, lessons-learned. Distinct from B1 / B3 by incident-time scope. | `nice` (Protect and Defend — Cyber Defense Analysis, Incident Response), `enisa` ECSF 1.0 (Cyber Incident Responder), `cis` v8 (Incident Response Management, Audit Log Management), `esco` (ICT — problem-resolution subset). | RFC-002 defaults; `confirm` gate on containment-action writes (isolate host, revoke key); **mandatory `block`** on public disclosure without legal sign-off and on evidence chain-of-custody breaks. | RFC-002 §8b grounding for incident claims; RFC-003 `verification_artifacts[]` for log / indicator refs (pointer-only); chain-of-custody attestation. | Lot B (compact index) | `candidate_mapped` |
| B26 | `sales-operator` | `x.klickd/sales_operator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/work` (P1)**) | `x.klickd/user`, `x.klickd/work` (P1) | B2B / SaaS sales operator running discovery, qualification, demo, proposal, close — CRM discipline, claim grounding, no-overpromise discipline. Distinct from marketing. | `esco` (S3 communicating / advising / mediating, S4.0.1), `onet` 28.0 (41-3091.00 Sales Representatives of Services; 41-4011.00 Technical / Scientific Sales cross-walk), `wef` 2023 ("leadership and social influence"), `lifecomp` (Social: empathy). | RFC-002 defaults; `confirm` gate on discount-floor overrides; **mandatory `block`** on cold outbound to a list without lawful basis and on product claims to a prospect without an evidence pointer. | RFC-002 §8b grounding for product claims; RFC-003 `verification_artifacts[]` for proof / case-study refs; pointer-only; no inline customer PII without consent. | Lot B (compact index) | `candidate_mapped` |
| B27 | `customer-support-operator` | `x.klickd/customer_support_operator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/work` (P1)**) | `x.klickd/user`, `x.klickd/work` (P1) | Customer-support / success operator running tier-1/2 triage, response, escalation — empathy, claim grounding, GDPR-aware data handling. NOT social media community management at scale. | `esco` (S3 communication; S1.2 interpersonal-communication support; S4.0.1), `onet` 28.0 (43-4051.00 Customer Service Representatives cross-walk), `lifecomp` (Social: empathy, collaboration), `wef` 2023 ("active learning"). | RFC-002 defaults; `confirm` gate on refund-floor overrides and on auto-close-without-customer-confirm writes; **mandatory `block` on cross-ticket PII sharing**. | RFC-002 §8b grounding for resolution claims; RFC-003 `verification_artifacts[]` for ticket / log refs (pointer-only); strict no-cross-ticket PII rule. | Lot B (compact index) | `candidate_mapped` |
| B28 | `finance-analyst` | `x.klickd/finance_analyst` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/work` (P1)**) | `x.klickd/user`, `x.klickd/work` (P1) | Corporate / FP&A finance analyst building forecasts, budgets, variance analyses for a company. NOT personal financial advice; NOT regulated investment advice. | `esco` (S4.0 management; S4.0.2 controlling / monitoring), `sfia` 8 (FMIT, METL — by id-link), `onet` 28.0 (13-2051.00 Financial and Investment Analysts cross-walk), `wef` 2023 ("analytical thinking"). | RFC-002 defaults; `confirm` gate on external forecast publication; **mandatory `block`** on silent model-assumption changes and on output resembling regulated investment advice (escalate to licensed advisor). | RFC-002 §8b grounding for forecast claims; RFC-003 `verification_artifacts[]` for model / assumption refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |
| B29 | `accounting-operator` | `x.klickd/accounting_operator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/work` (P1) and `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/work` (P1), `x.klickd/legal` (P0) | Accounting operator (bookkeeper, AP/AR, junior controller) maintaining ledgers, reconciliations, statutory filings. NOT a tax advisor; NOT a chartered auditor. | `esco` (S4.0.2 controlling / monitoring; S4.7 administration support), `onet` 28.0 (13-2011.00 Accountants and Auditors; 43-3031.00 Bookkeeping / Accounting Clerks cross-walks), `sfia` 8 (FMIT — by id-link). | RFC-002 defaults; `confirm` gate on statutory-filing submission; **mandatory `block`** on back-dated ledger entries outside declared periods and on output resembling regulated tax advice (escalate to tax advisor). | RFC-002 §8b grounding for ledger claims; RFC-003 `verification_artifacts[]` for receipt / invoice refs; pointer-only; no inline confidential transaction data. | Lot B (compact index) | `candidate_mapped` |
| B30 | `technical-writer` | `x.klickd/technical_writer` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/coding` (P0) and `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/coding` (P0), `x.klickd/research` (P0) | Technical writer producing API docs, runbooks, user guides, ADRs — audience modelling, source-of-truth discipline, version-aware references. | `esco` (S1.0 communication; S5.6 ICT — software-documentation subset), `sfia` 8 (TECH, HCEV — by id-link), `onet` 28.0 (27-3042.00 Technical Writers cross-walk), `digcomp` 2.2 (3.1 developing digital content; 3.3 copyright and licences). | RFC-002 defaults; `confirm` gate on third-party attribution writes; **mandatory `block`** on publishing against a stale source-of-truth and on doc-licence mismatch with cited third-party content. | RFC-002 §8b grounding for attribution claims; RFC-003 `verification_artifacts[]` for source-of-truth refs (pointer-only, version-pinned). | Lot B (compact index) | `candidate_mapped` |
| B31 | `learning-designer` | `x.klickd/learning_designer` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0) | Learning experience / instructional designer building curricula, courses, assessments — constructive alignment, accessibility, evidence-grounded outcomes. NOT a host-side tutor (that's `host_skill`). | `esco` (S1.6 training / coaching / teaching; S2.0 information skills), `digcomp` 2.2 (3.1 developing digital content; 5.3 creatively using digital technologies), `onet` 28.0 (25-9031.00 Instructional Coordinators cross-walk), `lifecomp` (Learning: growth mindset, managing learning). | RFC-002 defaults; `confirm` gate on high-stakes assessment publication; **mandatory `block`** on accessibility-review skip and on inline PII of minor learners. | RFC-002 §8b grounding for outcome claims; RFC-003 `verification_artifacts[]` for validity / accessibility evidence; pointer-only; no inline learner PII. | Lot B (compact index) | `candidate_mapped` |
| B32 | `sustainability-analyst` | `x.klickd/sustainability_analyst` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0) and `x.klickd/legal` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0), `x.klickd/legal` (P0) | Sustainability / ESG analyst running emissions accounting, CSRD/ESRS readiness, supplier-questionnaire analysis, materiality assessments. NOT a certified auditor. | `esco` (S4.8 sustainability; S2.0 information skills; S4.0.2 controlling / monitoring), `onet` 28.0 (13-1199.05 Sustainability Specialists; 19-2041.03 Industrial Ecologists cross-walks), `wef` 2023 ("environmental stewardship", "analytical thinking"). Citation-only: CSRD (Directive (EU) 2022/2464), ESRS (EFRAG), GHG Protocol (NOT registered as `frameworks[]`). | RFC-002 defaults; `confirm` gate on external sustainability claims; **mandatory `block`** on Scope-3 material omission and on unsubstantiated environmental claims (greenwashing). | RFC-002 §8b grounding for emissions / disclosure claims; RFC-003 `verification_artifacts[]` for methodology / data-source refs (pointer-only). | Lot B (compact index) | `candidate_mapped` |
| B33 | `healthcare-ai-safety-reviewer` | `x.klickd/healthcare_ai_safety_reviewer` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/research` (P0), `x.klickd/legal` (P0), `x.klickd/security` (P0)**) | `x.klickd/user`, `x.klickd/research` (P0), `x.klickd/legal` (P0), `x.klickd/security` (P0) | Reviewer auditing an AI system intended for a healthcare setting (clinical decision support, triage assistant, scribe) — maps risk under EU AI Act + MDR. NOT a clinician; NOT a notified body. | `esco` (S4.6 legal / compliance; S5.6 ICT — software-analysis subset; S2.0 information skills), `enisa` ECSF 1.0 (Cybersecurity Risk Manager), `nice` (Oversee and Govern — Strategic Planning and Policy). Citation-only: EU AI Act (Regulation (EU) 2024/1689), MDR (Regulation (EU) 2017/745), WHO Ethics and Governance of AI for Health (NOT registered as `frameworks[]`). | RFC-002 defaults; **mandatory `block`** on clinical-efficacy claims without trial / regulatory evidence, on MDR / AI-Act risk-class downgrade without evidence, and on patient-PII export outside declared lawful basis. | RFC-002 §8b grounding for risk-class claims; RFC-003 `verification_artifacts[]` for technical-documentation / trial refs; pointer-only; never inline patient PII. | Lot B (compact index) | `candidate_mapped` |
| B34 | `edge-ai-operator` | `x.klickd/edge_ai_operator` (**proposed new pack, requires P1+ RFC step; composes with `x.klickd/coding` (P0) and `x.klickd/security` (P0)**) | `x.klickd/user`, `x.klickd/coding` (P0), `x.klickd/security` (P0) | Engineer deploying and operating AI models on edge devices (mobile, embedded, robotics) — latency, power, model-quantisation hygiene, on-device evaluation. Distinct from `llm_agent_engineering` (B2) by cloud-vs-edge scope. | `esco` (S5.6.1 developing AI / ML systems; S5.5 software development), `sfia` 8 (PROG, HSIN, HCEV — by id-link), `onet` 28.0 (17-2199.10 Robotics Engineers cross-walk), `wef` 2023 ("AI and big data", "technology design and programming"). | RFC-002 defaults; `confirm` gate on safety-critical edge deploys (medical / vehicular / industrial); **mandatory `block`** on OTA deploys without rollback and on quantisation that silently drops accuracy below declared floor. | RFC-002 §8b grounding for on-device performance claims; RFC-003 `verification_artifacts[]` for evaluation / benchmark refs; pointer-only. | Lot B (compact index) | `candidate_mapped` |

### 2.1 Lot B notes & open questions

- **Compact-index loading is the size-tier discipline.** Lot B candidates that exceed Lot A's ~900-token ceiling MUST adopt the compact-index strategy (manifest top-level keys + framework IDs + competency IDs + gates + `human_authority` + `router_cost` in prompt; full bodies lazy). Any candidate whose compact index alone exceeds ~1,350 tokens MUST split into two packs rather than break the ceiling.
- **All Lot B candidates are `candidate_mapped`.** None is `ship_ready`. Each one needs its own per-pack RFC + scaffold under `docs/rfcs/chimera/packs/<name>.md` before any authoring. The order of authoring is **not** the order of this table; it is decided by the P1+ RFC sequencing in [`README_V4_1.md`](./README_V4_1.md) §3.
- **`agent-security` (B1), `iam-endpoint` (B3), `release-engineer` (B4) compose with `x.klickd/security` (P0).** They MUST NOT redefine or lower any default from `security`; they MAY raise gates only.
- **`eu-ai-act` (B6), `gdpr-readiness` (B7), `contract-review` (B8), `privacy-product` (B9), `rights-guard` (B18) compose with `x.klickd/legal` (P0).** Same rule — they MAY raise gates, never lower. Every one of them carries a mandatory escalation to qualified counsel for definitive decisions.
- **`evidence-desk` (B10), `policy-analyst` (B11), `second-brain` (B12), `literature-review` (B13) compose with `x.klickd/research` (P0).** The carrier-vs-skill rule of [RFC-009 §5.1.1](../rfcs/RFC-009-chimera-v4.1.md) applies — these packs carry the carrier's **state** (what they have done, what gates apply, what consent posture they hold); the matching **method** (how to investigate, how to write a brief, how to write atomic notes, how to run a PRISMA review) lives on the host side as a `host_skill` (`skill.research.assistant` for the generic case, with possible host-specific specialisations the host vendor chooses).
- **`drone` (B15), `mission-control` (B16)** introduce **operations-time-critical gates**. The `block` defaults above are minimums; reviewers MUST audit them before the candidates move past `candidate_mapped`.
- **`game-design` (B17), `rights-guard` (B18)** sit at the intersection of creator / legal / coding. They are listed separately so authors do not silently fold them into `x.klickd/creator` (P1) without an RFC step.
- **`video-production-pipeline` (B19) composes onto a candidate (`x.klickd/media_planner`, A4).** This is a candidate→candidate parent edge of the same shape as B2→B1 and B10→B5; downstream loaders that resolve parents transitively will chain candidates onto candidates at this stage. B19 raises strict `block` floors not present in A4: auto-publish-to-social, real-person likeness/voice, copyrighted-asset use without recorded rights, unsupported product-performance claims, and risky-provider use (e.g. g4f / pollinations). A4's `minor_in_frame` floor is preserved under composition. The pack carries `pipeline_checkpoints[]` declaring `stop_at` points for deterministic resume (script → storyboard → shot_list → prompt_list → voice_over → subtitles → render_plan → provenance → qa).

---

## 3. Excluded by design (NOT candidates for `/klickdskill`)

The following names appear in adjacent product / app contexts and **MUST NOT** be added to this candidate list, to the `/klickdskill` catalog, or to `examples/v4/starter-skills/`. They belong to the Klickd.app product and live under [`examples/v4/klickdapp-skills/`](../../examples/v4/klickdapp-skills/) only.

| Excluded name | Why excluded |
|---|---|
| `klickdapp.lu.klickd` | Klickd.app country-scoped student carrier (LU). Product-side, not public-spec. |
| `klickdapp.fr.klickd` | Klickd.app country-scoped student carrier (FR). Product-side, not public-spec. |
| `klickdapp.be.klickd` | Klickd.app country-scoped student carrier (BE). Product-side, not public-spec. |
| `klickdapp.de.klickd` | Klickd.app country-scoped student carrier (DE). Product-side, not public-spec. |
| `Kai Tutor`, `kai.tutor`, `skill.kai.tutor.socratic` | Host-side `host_skill` for the Klickd / Kai LLM. Not a `carrier_pack`. Lives in the host runtime, not in any user file. |
| `Kai Mentor`, any other `kai.*` | Host-side. Same as above. |
| `core.Kai.klickd`, `core.<Agent>.klickd` | Agent-core artefacts under [RFC-006](../rfcs/RFC-006-agent-core.md). Not `carrier_pack`s. Composition rule: `core.*` constrains, `x.klickd/<pack>` declares competence, `user.klickd` personalises. |

The `/klickdskill` public catalog (when and only when it ships per [RFC-009 §7](../rfcs/RFC-009-chimera-v4.1.md)) MUST NOT list any of the names above as a candidate Chimera pack. A planning document under `docs/chimera/` that does the same is a §8 / `forbidden_fields` failure (see [`V4_1_CANDIDATE_CHECKLIST.md`](./V4_1_CANDIDATE_CHECKLIST.md) check C-006).

---

## 4. Summary by status

| Status | Lot A | Lot B | Total |
|---|---|---|---|
| `candidate_mapped` | A1, A2, A3, A4, A7, A9, A10, A11, A12, A13 (10) | B1, B2, B3, B4, B5, B6, B7, B8, B9, B10, B11, B12, B13, B14, B15, B16, B17, B18, B19, B20, B21, B22, B23, B24, B25, B26, B27, B28, B29, B30, B31, B32, B33, B34 (34) | **44** |
| `needs_mapping` | A5, A6, A8, A14, A15 (5) | — (0) | **5** |
| `ship_ready` | — | — | **0** |

> Of the 44 `candidate_mapped` rows above, **42** have a concrete `.klickd` artefact on disk (8 Lite — A1, A4, A7, A9, A10, A11, A12, A13 — and 34 Pro — B1..B34). A2 `language` and A3 `exam` are sub-areas of `x.klickd/student` (not separate packs) and therefore carry no artefact; they remain documented here so reviewers do not propose them as siblings. The artefact count (8 Lite + 34 Pro = **42**) is frozen and enforced by the validator (`TIER_EXPECTED_COUNT`).

Per the 2026-05-27 audit response (§0.5), **A8 `crypto-lite` moved from `candidate_mapped` to `needs_mapping`** — no EU SKOS-published crypto-asset-literacy framework with DigComp / NICE-comparable maturity exists; under the user's strict rule "no invented names not grounded in Chimera mapping" the artefact was removed and the row stays as a deferred-planning entry.

Promotion path:

1. `needs_mapping` → `candidate_mapped` happens when the framework anchor question (financial-literacy / health-literacy / family-coordination / PII-of-others consent shape) is resolved at RFC level.
2. `candidate_mapped` → `ship_ready` happens when the candidate has a scaffold under `docs/rfcs/chimera/packs/<name>.md`, framework anchors fully resolved, offline SKOS/JSON-LD bundle bytes, strict JSON Schema, round-trip vector, deterministic `router_cost`, static `forbidden_fields` check, and pair-reviewer sign-off — i.e. all ten criteria of [RFC-009 §8](../rfcs/RFC-009-chimera-v4.1.md).
3. `ship_ready` → `catalog_exposed` is a **separate** decision per [RFC-009 §7](../rfcs/RFC-009-chimera-v4.1.md). Passing validation does not trigger catalog exposure.

---

## 5. See also

- [`docs/rfcs/RFC-009-chimera-v4.1.md`](../rfcs/RFC-009-chimera-v4.1.md) — Chimera RFC (P0 / P1 set, validation §8, no-catalog §7, carrier-vs-skill §5.1.1, clean-architecture §5.0).
- [`docs/rfcs/chimera/README.md`](../rfcs/chimera/README.md) — Chimera companion summary.
- [`docs/rfcs/chimera/packs/README.md`](../rfcs/chimera/packs/README.md) — concrete pack scaffold index.
- [`docs/rfcs/chimera/frameworks/README.md`](../rfcs/chimera/frameworks/README.md) — canonical framework registry.
- [`docs/rfcs/chimera/packs/router_cost.md`](../rfcs/chimera/packs/router_cost.md) — heuristic token-cost rows for already-mapped packs.
- [`docs/chimera/README_V4_1.md`](./README_V4_1.md) — planning index for v4.1.
- [`docs/chimera/V4_1_CANDIDATE_CHECKLIST.md`](./V4_1_CANDIDATE_CHECKLIST.md) — per-candidate review checklist.
- [`docs/chimera/V4_1_COMPETENCY_IDENTIFICATION_PROTOCOL.md`](./V4_1_COMPETENCY_IDENTIFICATION_PROTOCOL.md) — formal method for selecting and combining competencies per skill (transversal base + domain-specific; coherence and anti-clone rules; validator-enforced subset). **Merge gate for the v4.1 candidate set.**
- [`examples/v4/starter-skills/`](../../examples/v4/starter-skills/) — already-shipped P0 starter `.klickd` files (`user`, `student`, `research`, `coding`). NOT extended by this document.
- [`examples/v4.1/x-klickd-skills/`](../../examples/v4.1/x-klickd-skills/) — **real `candidate_mapped` artefacts** produced from this mapping. `lite/` (8 packs, after the 2026-05-27 audit-response `crypto-lite` deferral) and `pro/` (34 packs after the 2026-05-27 v4.1 expansion that added B20..B34 on top of the prior 19), each with a `manifest.json` carrying `sha256_file` + `sha256_canonical_json` per pack. Frozen at **8 Lite + 34 Pro = 42** artefacts; the validator (`scripts/validate_v4_1_candidate_mapping.py`, `TIER_EXPECTED_COUNT`) rejects any drift.
- [`examples/v4/klickdapp-skills/`](../../examples/v4/klickdapp-skills/) — Klickd.app product carriers. Out of scope for this document; **never** referenced as candidate Chimera packs.
